import { ApproveexpPipe } from './approveexp.pipe';

describe('ApproveexpPipe', () => {
  it('create an instance', () => {
    const pipe = new ApproveexpPipe();
    expect(pipe).toBeTruthy();
  });
});
